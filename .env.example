# Vendor secrets — populate locally, never commit the real .env
# (Not consumed by the Pflichtregeln slice, kept here for future vendor slices.)
ORATS_TOKEN=
FMP_KEY=
IVOLATILITY_API_KEY=
GOOGLE_SHEET_ID=

# Logging
LOG_LEVEL=INFO
